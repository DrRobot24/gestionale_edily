/**
 * SEGNAPOSTO. Questo file va RIGENERATO, non scritto a mano:
 *
 *   npx supabase gen types typescript --linked > src/lib/database.types.ts
 *
 * Finche' resta `any` il codice compila ma non hai autocompletamento sui
 * nomi delle tabelle e delle colonne. Rigeneralo dopo ogni migration:
 * e' il modo per accorgerti a compile-time che hai rinominato una colonna.
 */
export type Database = any
